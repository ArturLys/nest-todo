services:
  - type: web
    name: nest-todo-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    port: 3000
    plan: free
    envVars:
      - key: PORT
        value: 3000
      - key: DATABASE_URL
        value: file:./dev.db
